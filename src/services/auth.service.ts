// Auth service — Phase 1 implementation
// Handles OTP generation/verification and JWT token lifecycle

export class AuthService {
  // TODO Phase 1: sendOtp(phone: string): Promise<void>
  // TODO Phase 1: verifyOtp(phone: string, otp: string): Promise<TokenPair>
  // TODO Phase 1: refreshAccessToken(refreshToken: string): Promise<{ accessToken: string }>
  // TODO Phase 1: revokeRefreshToken(userId: string): Promise<void>
}

export const authService = new AuthService();
