// Chat service — Phase 4 implementation

export class ChatService {
  // TODO Phase 4: getMessages(chatId: string, chatType: ChatType, query: PaginationQuery): Promise<PaginatedResponse<IMessage>>
  // TODO Phase 4: sendMessage(chatId: string, senderId: string, content: string, chatType: ChatType): Promise<IMessage>
  // TODO Phase 4: markRead(chatId: string, coupleId: string): Promise<void>
}

export const chatService = new ChatService();
