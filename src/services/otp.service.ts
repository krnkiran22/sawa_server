// OTP service — Phase 1 implementation

export class OtpService {
  // TODO Phase 1: generate(phone: string): Promise<string> — returns raw OTP, stores hash
  // TODO Phase 1: verify(phone: string, otp: string): Promise<boolean>
  // TODO Phase 1: invalidate(phone: string): Promise<void>
  // TODO Phase 1: send(phone: string, otp: string): Promise<void> — Twilio integration
}

export const otpService = new OtpService();
