// Community service — Phase 4 implementation

export class CommunityService {
  // TODO Phase 4: list(query: PaginationQuery): Promise<PaginatedResponse<ICommunity>>
  // TODO Phase 4: create(data: Partial<ICommunity>, creatorId: string): Promise<ICommunity>
  // TODO Phase 4: findById(id: string): Promise<ICommunity | null>
  // TODO Phase 4: join(communityId: string, coupleId: string): Promise<void>
  // TODO Phase 4: leave(communityId: string, coupleId: string): Promise<void>
  // TODO Phase 4: findByCoupleId(coupleId: string): Promise<ICommunity[]>
}

export const communityService = new CommunityService();
