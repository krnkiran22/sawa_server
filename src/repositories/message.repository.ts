// Message repository — Phase 4 implementation

export class MessageRepository {
  // TODO Phase 4: create(data: Partial<IMessage>): Promise<IMessage>
  // TODO Phase 4: findByChatId(chatId: string, query: PaginationQuery): Promise<{ data: IMessage[], total: number }>
  // TODO Phase 4: markRead(chatId: string, coupleId: string): Promise<void>
}

export const messageRepository = new MessageRepository();
