// Couple repository — Phase 2 implementation

export class CoupleRepository {
  // TODO Phase 2: create(data: Partial<ICouple>): Promise<ICouple>
  // TODO Phase 2: findByPartnerId(userId: string): Promise<ICouple | null>
  // TODO Phase 2: findById(id: string): Promise<ICouple | null>
  // TODO Phase 2: update(id: string, data: Partial<ICouple>): Promise<ICouple>
  // TODO Phase 2: findExcluding(excludeIds: string[], limit: number): Promise<ICouple[]>
}

export const coupleRepository = new CoupleRepository();
